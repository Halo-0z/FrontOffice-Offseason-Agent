"""Transaction models.

Future contents (M2):

- ``TransactionProposal``        : a signing or trade proposal with
  team(s), player(s), terms, and proposal type.
- ``TransactionValidationResult``: deterministic verdict from
  ``transaction_rule_engine`` (valid / invalid + reason codes).

M0 scope: module docstring + TODO only. No logic.
"""

# TODO(M2): define TransactionProposal dataclass (signing | trade).
# TODO(M2): define TransactionValidationResult dataclass with reason codes.
