"""Salary cap models.

Future contents (M1):

- ``SalaryCapConfig``  : league-wide cap parameters (salary cap, tax line,
  first/second apron, rookie scale, mid-level exceptions, etc.).
- ``TeamCapSheet``     : a single team's cap sheet: guaranteed salaries,
  cap holds, exceptions, current cap space, distance to each apron.
- ``PlayerContract``   : a player's contract: years, annual salary,
  guarantees, options, trade kickers, cap hit per season.

M0 scope: module docstring + TODO only. No logic.
"""

# TODO(M1): define SalaryCapConfig dataclass.
# TODO(M1): define TeamCapSheet dataclass with cap_space, apron_distance.
# TODO(M1): define PlayerContract dataclass with per-season cap hit.
