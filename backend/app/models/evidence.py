"""Evidence models.

Future contents (M3):

- ``EvidenceNote``      : a single piece of evidence with id, source,
  summary, tags, and confidence.
- ``EvidenceReference`` : a citation linking a plan/transaction to one
  or more ``EvidenceNote`` ids.

M0 scope: module docstring + TODO only. No logic.
"""

# TODO(M3): define EvidenceNote dataclass.
# TODO(M3): define EvidenceReference dataclass linking plans -> evidence_ids.
