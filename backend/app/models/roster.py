"""Roster models.

Future contents (M1/M3):

- ``Player``      : id, name, position, role, height, age, contract ref.
- ``TeamRoster``  : list of players for a team, roster size, slot usage.
- ``DepthChart``  : position -> ordered player ids (starter + backups).

M0 scope: module docstring + TODO only. No logic.
"""

# TODO(M1): define Player dataclass.
# TODO(M1): define TeamRoster dataclass with roster_size and slot usage.
# TODO(M3): define DepthChart dataclass and projection helpers.
