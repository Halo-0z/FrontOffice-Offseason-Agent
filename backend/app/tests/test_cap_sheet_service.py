"""Tests for ``cap_sheet_service``.

Planned cases (see docs/evaluation.md):

- cap space enough, signing passes
- cap space insufficient, signing fails

Milestone: M1.

M0 scope: TODO stubs only.
"""

# TODO(M1): test_load_team_cap_sheet_returns_expected_space.
# TODO(M1): test_signing_passes_when_cap_space_sufficient.
# TODO(M1): test_signing_fails_when_cap_space_insufficient.
# TODO(M1): test_apron_distance_computed_correctly.
