"""Evidence service.

Responsibility: read evidence notes and resolve ``evidence_id``
references so the agent can cite supporting evidence for each plan.

Milestone: M3.

M0 scope: docstring + TODO only.
"""

# TODO(M3): implement load_evidence() -> dict[evidence_id, EvidenceNote].
# TODO(M3): implement attach(plan, evidence_ids) -> plan with resolved refs.
# TODO(M3): read inputs from data/evidence_notes.json.
