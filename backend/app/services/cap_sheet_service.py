"""Cap sheet service.

Responsibility: compute and expose a team's cap sheet, including cap
space, apron distances, guaranteed salaries, cap holds, and exceptions.
This is the single source of truth for salary state.

Milestone: M1.

M0 scope: docstring + TODO only.
"""

# TODO(M1): implement load_team_cap_sheet(team_id) -> TeamCapSheet.
# TODO(M1): implement cap_space(team_id) -> int.
# TODO(M1): implement apron_distance(team_id, apron) -> int.
# TODO(M1): implement apply_signing(sheet, contract) -> TeamCapSheet (pure).
# TODO(M1): read inputs from data/contracts.json and data/teams.json.
