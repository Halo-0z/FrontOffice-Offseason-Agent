"""Depth chart projector.

Responsibility: project the post-transaction depth chart for the
upcoming season given a team's roster and a set of applied transactions.

Milestone: M3.

M0 scope: docstring + TODO only.
"""

# TODO(M3): implement project(team_id, applied_transactions) -> DepthChart.
# TODO(M3): position -> ordered player ids (starter + backups).
# TODO(M3): pure function; does not mutate roster state.
