"""Roster need service.

Responsibility: analyze a team's roster and identify positional/role
shortfalls (e.g. "no starting PG", "lack of floor spacing wings").

Milestone: M3.

M0 scope: docstring + TODO only.
"""

# TODO(M3): implement load_roster(team_id) -> TeamRoster.
# TODO(M3): implement analyze_needs(roster) -> list[Need].
# TODO(M3): Need shape: {position, role, severity, rationale}.
