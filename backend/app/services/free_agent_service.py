"""Free agent service.

Responsibility: read the free-agent pool and return candidate targets
filtered by team need and cap feasibility.

Milestone: M3.

M0 scope: docstring + TODO only.
"""

# TODO(M3): implement candidates(team_id, needs, cap_sheet) -> list[FreeAgentCandidate].
# TODO(M3): read inputs from data/free_agents.json.
# TODO(M3): filter by position need, expected salary vs cap space.
