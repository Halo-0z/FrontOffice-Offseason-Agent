"""Trade simulator.

Responsibility: simulate two-team trades. Check salary matching, asset
exchange, and post-trade cap impact for both sides. Returns candidate
trade proposals for the rule engine to validate.

Milestone: M3.

M0 scope: docstring + TODO only.
"""

# TODO(M3): implement candidates(team_id, needs, cap_sheet) -> list[TradeCandidate].
# TODO(M3): implement match_salaries(outgoing, incoming) -> bool (heuristic).
# TODO(M3): never mutate state; return proposal objects only.
