# FrontOffice-Offseason-Agent backend dependencies.
#
# Minimal manifest: only what the backend (services + API + CLI demos)
# and the test suite need. No LLM, no MCP, no NBA API, no database, no
# ORM, no web-scraping libraries.
#
# Install (Windows PowerShell, Python 3.12+):
#
#     D:\anaconda\python.exe -m pip install -r requirements.txt
#
# The version ranges are intentionally loose (compatible-release) so
# the project is reproducible without over-locking to a specific patch.

# --- API layer (M7-A) ---
fastapi>=0.115,<1.0          # API framework for backend/app/api.py
uvicorn>=0.30,<1.0           # ASGI server for local dev (uvicorn backend.app.api:app)
pydantic>=2.0,<3.0           # Request/response validation in FastAPI

# --- HTTP test client (used by TestClient in test_api_endpoints.py) ---
httpx>=0.27,<1.0             # FastAPI TestClient transport

# --- Test runner ---
pytest>=8.0,<9.0             # Backend test suite (backend/app/tests)
pytest-asyncio>=0.24,<1.0    # Async test support (used by the environment)
